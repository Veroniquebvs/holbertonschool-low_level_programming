#include "main.h"
#include <stdio.h>

/**
 * _strlen - renvoie la longueur d'une chaine
 * @s: Le nombre à échanger
 *
 *Return: la longueur de la chaine
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}

	return (i);
}
